FileStateStore.Save marshals State to indented XML and writes it to .drift/state.xml, truncating any existing file. It also ensures .drift/baselines/ exists. The XML root element is drift (version 4) with nested specs, markers, and edges sections.