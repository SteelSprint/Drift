<module name="build">
<spec id="list_result">
Overview: BuildListResult constructs a ListResult by resolving file content for each spec and marker when verbose mode is requested. Presenters consume the fully-resolved result; they never perform I/O themselves.

Defined terms:
  verbose — when true, content previews are read from disk and stored in SpecContents / MarkerContents maps; when false, both maps are nil

Requirements:
  R1. BuildListResult MUST skip specs and markers marked Deleted (no content read attempted for missing files).
  R2. When verbose is false, BuildListResult MUST return a ListResult with nil SpecContents and nil MarkerContents. The presenter renders the compact view.
  R3. When verbose is true, BuildListResult MUST populate SpecContents[specID] and MarkerContents[markerID] for every non-deleted spec and marker. Entries whose content read fails MUST be silently omitted (partial result is preferred over hard failure for list output).
  R4. BuildListResult MUST NOT return an error; content-read failures are best-effort.

See also: cli.list_format.
</spec>

<spec id="show_result">
Overview: BuildShowResult constructs a ShowResult containing the full citation closure reachable from the seed (a spec or marker ID). The closure walks spec-spec edges in both directions to fixpoint via BFS, includes every marker linked to any reached spec, and resolves content for every node. Used by `drift show &lt;id&gt;`.

Defined terms:
  isSpec — heuristic: ID contains exactly one dot → spec; no dot → marker
  closure — the full transitive set of nodes reachable by walking spec-spec edges bidirectionally from the seed, plus markers linked to any reached spec

Requirements:
  R1. BuildShowResult MUST dispatch on isSpecID(id) to determine whether the seed is a spec or marker. Spec IDs contain a dot; marker IDs do not.
  R2. If the seed entity is not found in state, BuildShowResult MUST return a ShowResult with empty Nodes and a nil error. The caller (drift show command) checks EntityExists to set exit code 1.
  R3. BuildShowResult MUST resolve content for every node in the closure. Content reads that fail (e.g. deleted files) MUST be silently omitted — the node is included with empty Content. This is a best-effort resolution, not a hard failure.
  R4. For a marker seed, BuildShowResult MUST expand the initial reachability set to include the marker's linked specs (outgoing edge targets) before starting the BFS, so the closure includes the specs the marker implements.
  R5. The BFS MUST walk both outgoing and incoming spec-spec edges to fixpoint, so the closure contains the full citation neighborhood (ancestors and descendants).
  R6. Nodes and edges in the ShowResult MUST be sorted by ID and by (From, To) respectively for deterministic output.

See also: cli.show_command.
</spec>

<spec id="edge_predicates">
Overview: EntityExists and DiffEdgeExists query state for the existence of nodes and edges, used by command dispatch to set exit codes without constructing full results.

Requirements:
  R1. EntityExists(state, id) MUST return true if and only if id matches a spec ID (when id contains a dot) or a marker ID (when id has no dot) in state.
  R2. DiffEdgeExists(state, markerID, specID) MUST return true if and only if both the named marker and the named spec exist in state.
  R3. sortEdgesByFromTo MUST sort edges lexicographically by (From, To) using insertion sort. The sort MUST be stable to preserve deterministic output across state-store iteration orders.

See also: cli.show_command, cli.list_format.
</spec>
</module>
