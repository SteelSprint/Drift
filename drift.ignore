# Exclude test fixture file (contains D! marker literals for test data)
internal/testutil/fixtures.go

# Exclude init template (a fixture copied into new projects, not this repo's specs)
cli/init_main.drift.xml

# Exclude test files (contain D! marker literals in test cases)
*_test.go

# Exclude examples (illustrative, not real specs to track)
examples/

# Exclude eval (pipeline.go contains D! marker literals in prompt strings)
eval/

# Exclude third-party tool dependencies (vendored, never spec-eligible)
.opencode/
node_modules/

# Exclude VCS
.git/

# Exclude drift state directory
.drift/
