# Exclude test fixture file (contains D! marker literals for test data)
internal/testutil/fixtures.go

# Exclude examples (use new <module> format not yet supported by scanner)
examples/

# Exclude eval (pipeline.go contains D! marker literals in prompt strings)
eval/

# Exclude VCS
.git/
